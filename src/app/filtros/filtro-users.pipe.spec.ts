import { FiltroUsersPipe } from './filtro-users.pipe';

describe('FiltroUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
